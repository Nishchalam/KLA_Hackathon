[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kla_hackathon"
version = "0.1.0"
description = "KLA Hackathon project"
authors = [ { name = "" } ]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
]
